{% if entrant %}

    {% if entrant.isSinglePlayer %}

        <a href="{{ url('player_details', { 'slug': entrant.firstPlayer.playerProfile.slug }) }}">
            {{ entrant.nameWithPlayers }}
        </a>

    {% elseif entrant.isTeam %}

        {{ entrant.name }} (

        {% for player in entrant.getPlayers() %}

            <a href="{{ url('player_details', { 'slug': player.playerProfile.slug }) }}">
                {{ player.playerProfile.gamerTag }}
            </a>

        {% endfor %}

        )

    {% else %}

        {{ entrant.name }}

    {% endif %}

{% endif %}
